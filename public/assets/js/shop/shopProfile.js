document.addEventListener("DOMContentLoaded", function () {
    ModalPopOver(
        "add-image-open-btn",
        "add-image-modal",
        "add-image-modal-content",
        "add-image-modal-close-btn"
    );
});
